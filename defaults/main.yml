---

proxmox_kvm_specs: []
proxmox_nic_specs: []
proxmox_disk_specs: []

proxmox_connection_info:
  api_user: "{{ proxmox_api_user }}"
  api_password: "{{ proxmox_api_password }}"
  api_host: "{{ proxmox_host_ip }}"

proxmox_spec_override: {}
